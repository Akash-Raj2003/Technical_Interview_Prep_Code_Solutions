class Solution:
    def hasDuplicate(self, nums: List[int]) -> bool:

        HashSet = {}

        for i in nums:
            if i in HashSet:
                return True
            else:
                HashSet[i] = 1
        
        return False


   

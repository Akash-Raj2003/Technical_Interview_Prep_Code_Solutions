class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:

        Seen = {}

        for i in range(len(nums)):
            x = target - nums[i]
            if x in Seen:
                return [Seen[x], i]
            else:
                Seen[nums[i]] = i
        
